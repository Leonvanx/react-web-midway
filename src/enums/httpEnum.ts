/**
 * @description: Request result set
 */
export enum GlobalResultCodeEnum {
  SUCCESS = 0,
  ERROR = -1,
  TIMEOUT = 401,
}

export enum GlobalResultMessageEnum {
  SUCCESS = '成功！',
  ERROR = '服务器错误！',
}
